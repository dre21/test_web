stages:
- name: Publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: httpd:${CICD_EXECUTION_SEQUENCE}
      pushRemote: false
      registry: 127.0.0.1:32443
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
