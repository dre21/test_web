stages:
- name: Publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: httpd:${CICD_EXECUTION_SEQUENCE}
      pushRemote: true
      registry: 10.240.0.72
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
